// const jwt = require('jsonwebtoken');

// const generateToken = (userId) => {
//   const secretKey = 'sihenceBoyCodes'
//   const token = jwt.sign({ userId }, secretKey, { expiresIn: '1h' }); 
//   return token;
// };

// module.exports = { generateToken };
